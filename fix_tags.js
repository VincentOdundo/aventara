const fs = require('fs'); const path = require('path'); const files = fs.readdirSync('src/pages').filter(f=>f.endsWith('.jsx')); files.forEach(f => { const p = path.join('src/pages', f); let code = fs.readFileSync(p, 'utf-8'); code = code.replace(/<hr([^>]*)>/gi, (m,p1)=> p1.endsWith('/') ? m : \<hr\ />\); code = code.replace(/<input([^>]*)>/gi, (m,p1)=> p1.endsWith('/') ? m : \<input\ />\); code = code.replace(/<img([^>]*)>/gi, (m,p1)=> p1.endsWith('/') ? m : \<img\ />\); fs.writeFileSync(p, code); }); console.log('Fixed tags');
